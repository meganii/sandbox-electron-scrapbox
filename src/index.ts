const { mount } = await import("https://raw.githubusercontent.com/meganii/ScrapBubble/main/mod.tsx")
await mount({
    whiteList: [
        "meganii",
        "meganii-mediamarker",
        "meganii-database",
        "meganii-network"
    ]
})
